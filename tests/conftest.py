"""Test package conftest.

The disable flag for the plugin's eager-start lives in the project-root
``conftest.py`` — see the comment there for why.  This module is kept
empty so pytest still treats ``tests/`` as a rootdir candidate.
"""
